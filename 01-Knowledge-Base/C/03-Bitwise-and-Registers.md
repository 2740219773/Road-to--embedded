# 位运算与寄存器

硬件寄存器通常由多个 bit 字段组成，因此嵌入式 C 大量使用位运算。

```c
reg |=  (1U << 5);   // 置位 bit5
reg &= ~(1U << 5);   // 清除 bit5
reg ^=  (1U << 5);   // 翻转 bit5
```

最重要的工程区别之一：

```c
reg = (1U << 5);
```

会整体重写寄存器；而 `|=` 通常用于保留其他位并置位目标 bit。

这类知识最适合通过 32-bit Register Playground 学习，而不是只背运算符。
