# struct、enum 与 typedef

`struct` 把相关数据组织成一个对象；`enum` 给离散状态建立有意义的名字；`typedef` 为类型建立别名。

嵌入式中结构体尤其重要，因为芯片厂商常用结构体描述一组连续寄存器：

```c
GPIOA->ODR
```

表面上像访问对象字段，底层可能对应对固定外设地址的寄存器访问。

因此结构体不仅用于“保存学生信息”，还直接参与硬件抽象、协议数据和驱动设计。
