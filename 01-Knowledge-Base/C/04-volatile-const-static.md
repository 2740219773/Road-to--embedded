# volatile、const 与 static

## volatile
告诉编译器：对象可能被程序当前代码流之外的因素改变，每次访问都需要按语言和实现规则实际读取/写入。常见于硬件寄存器、ISR 与主流程共享状态等场景。

`volatile` 不等于线程安全，也不自动提供原子性或内存同步。

## const
表达通过当前接口不应修改对象，可用于保护只读配置和 API 约束。

## static
根据位置具有不同含义：函数内 static 对象具有静态存储期；文件作用域 static 名称通常限制在当前翻译单元。

学习时应通过“去掉关键词会出现什么现象”理解，而不是只背定义。
