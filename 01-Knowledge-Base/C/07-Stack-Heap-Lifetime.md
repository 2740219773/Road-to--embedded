# 栈、堆与对象生命周期

嵌入式设备 RAM 往往有限，因此必须理解对象何时存在、占用哪里的空间。

栈常用于函数调用和自动变量；堆用于动态分配；全局和 static 对象具有静态存储期。

典型故障包括：栈溢出、返回局部变量地址、内存泄漏、越界写导致其他状态被破坏。

学习时应结合 debugger 的 memory/stack 观察，而不是把“栈向下、堆向上”当作唯一结论；具体布局取决于平台和链接配置。

