# 指针与硬件

指针是保存地址的变量。

```c
int value = 10;
int *p = &value;
*p = 20;
```

`p` 保存 `value` 的地址，`*p` 表示访问该地址中的对象。

嵌入式中的关键连接：

```text
指针
↓
地址
↓
Memory-mapped Register
↓
硬件外设
```

芯片厂商的头文件会把大量寄存器地址封装成结构体和宏，但底层思想仍然是通过地址访问硬件。

不要随意访问任意地址；非法地址、权限或对齐问题都可能导致异常。
