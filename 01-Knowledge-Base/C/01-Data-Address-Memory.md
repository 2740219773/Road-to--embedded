# 数据、地址与内存

程序中的数据最终需要存在内存中。为了找到某一位置，处理器使用地址。

```text
地址        内容
0x1000      10
0x1004      25
```

变量可以理解为程序给某块存储位置起的名字；`&value` 获取地址。

在嵌入式系统中，这个概念进一步延伸到 Memory-mapped I/O：某些地址不是普通 RAM，而对应真实硬件寄存器。

因此“地址”是理解指针、寄存器、外设和调试器内存窗口的共同基础。

互动入口：`04-Missions/Phase-1-C/01-Memory-Detective/Mission.md`。
