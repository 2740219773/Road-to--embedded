# CPU、MCU、SoC 与 FPGA

## CPU
执行指令的处理核心。

## MCU
把 CPU、Flash、RAM、GPIO、Timer、UART、SPI、ADC 等资源集成在一颗芯片中，适合控制类嵌入式系统。

## SoC
集成度更高的系统级芯片，常包含多核 CPU、复杂外设、图形或高速接口，可运行 Linux 等复杂系统。

## FPGA
不是主要依靠顺序执行软件指令完成任务，而是通过可编程逻辑构造并行数字电路。

学习路线不应该把它们理解成简单的“高级/低级关系”。它们解决的问题不同，并经常在同一个系统中协作。

