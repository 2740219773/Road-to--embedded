# 第一次建立系统调试思维

调试不是反复改代码直到碰巧正常。

推荐基本流程：

```text
现象
↓
明确预期
↓
提出假设
↓
寻找可观测证据
↓
逐个排除
↓
找到根因
↓
修复
↓
重新验证
```

例如 LED 不亮，不要立刻认为程序错了。可以检查程序是否运行、GPIO 时钟、引脚模式、寄存器状态、引脚电压、LED 极性和硬件连接。

Road to Embedded 会把这种方法贯穿每个阶段。
