# Embedded Linux Knowledge Base

这里保存 Embedded Linux 的技术知识底稿。

计划内容：

- Shell / filesystem / process / thread；
- GCC / Make / CMake / cross compile；
- Bootloader；
- Kernel；
- Device Tree；
- Driver；
- GPIO / I2C / SPI / UART；
- Buildroot / Yocto；
- user space application。

当前旧 `04-Embedded-Linux/` 内容将在 V2.1 后续迁移到这里。